import { getName } from "./test2";

var foo = 1;
if(true){
    foo = 1;
}

let huhu = {name: 'Hurz'}

function bar(val:number): boolean | number{
    if(val <= 7){
        return true
    } else {
        return 22
    }
}

const word = `Der wert ist ${huhu.name}`
huhu.name = 'Das Lamm'

const root = 'https://kjdhflskj.fv/ödslfikhdjafpö'
let url = `${root}/6546` // root + '/6546'

getName()
[].splice()